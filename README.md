# Design implementation test

To run the project, run the following commands :

```bash
# install dependencies
yarn install

# Build the project
yarn tailwind:build
yarn build

# Launch web server
yarn preview
```
