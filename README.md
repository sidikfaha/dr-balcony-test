# Design implementation test

Tu run the project, run the followed commands :

```bash
# install dependencies
yarn install

# Build the project
yarn tailwind:build
yarn build

# Launch web server
yarn preview
```
